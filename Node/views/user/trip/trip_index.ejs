<!doctype html>
<html lang="ko">
<head>
    <title>여행목록</title>
    <link rel="shortcut icon" type="image⁄x-icon" href="https://img.icons8.com/doodle/48/000000/log-cabin.png">
    <meta charset="UTF-8">
    <meta name="viewport"
          content="width=device-width, user-scalable=no, initial-scale=1.0, maximum-scale=1.0, minimum-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <!--    폰트-->
    <link href="https://fonts.googleapis.com/css2?family=Noto+Sans+KR:wght@400&display=swap" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Nanum+Gothic&display=swap" rel="stylesheet">

    <link rel="stylesheet" type="text/css" href="/static/css/user/trip/trip_index.css"/>
    <link rel="stylesheet" type="text/css" href="/static/css/nav.css"/>
</head>
<body>
<ul class="top_nav">
    <li id="logo"><a href="/" class="active"><img class="logo_image"
                                                  src="https://img.icons8.com/doodle/48/000000/log-cabin.png"></a></li>
    <div id="navAlign">
        <li><a href="/message">메시지</a></li>
        <li><a>여행</a></li>
        <li><a><%= nickname %></a></li>
        <li><a href="/logout">로그아웃</a></li>
    </div>
</ul>

<div class="row">
    <div class="column">
        <div class="item" style="border: 0px">
            <div class="item_title">여행</div>
            <% if(rooms.length !== 0) { %>
                <% let ck = 0; %>
                <% for(let i = 0; i < rooms.length; i++) { %>
                    <% if (i % 3 === 0) { %>
                    <div class="tray">
                        <% } %>
                        <div class="card" onclick="detailTrip('<%= rooms[i].aid %>')">
                            <img src="/static/images/rooms/<%= rooms[i].image %>"/>
                            <div class="card_info">
                                <div class="date"><%= rooms[i].date.startDay %> - <%= rooms[i].date.endDay %></div>
                                <b><%= rooms[i].region %></b>
                                <p style="font-size: 14px"><%= rooms[i].item_name %></p>
                            </div>
                        </div>
                        <% if (++ck % 3 === 0){ %>
                    </div>
                    <% } %>
                <% } %>
            <% } else{ %>
                <div class="tray">
                    <div class="empty_card">
                        아직 예약된 내역이 없네요
                    </div>
                </div>
            <% } %>
        </div>
    </div>
</div>

<!--모달처리 JS-->
<script src="/static/js/modal.js"></script>
<!--여행관련 JS-->
<script src="/static/js/user/trip.js"></script>
</body>
</html>
